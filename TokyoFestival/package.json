{
  "name": "tokyo-festival",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "prebuild": "vite build --base=/tokyo/",
    "preview": "vite preview",
    "deploy": "npx gh-pages -d dist -b build --dest ./tokyo"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.1",
    "@types/gh-pages": "^6.1.0",
    "gh-pages": "^6.1.1",
    "svelte": "^4.2.8",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "lucide-svelte": "^0.298.0"
  }
}
